<article class="movie-info">
  <img
    src="{{movie.attributes.image.url}}"
    alt="{{movie.attributes.title}}"
  >

  <h1>{{movie.attributes.title}}</h1>

  <p>{{movie.attributes.intro}}</p>

  <a href="/movies">← Tillbaka till filmer</a>
</article>